pipeline {
    agent any

    stages {
        stage("Run Custom Plugin") {
            steps {
                runMyPlugin param1: 'value1', param2: 'value2'
            }
        }
    }
}
